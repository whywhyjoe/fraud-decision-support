# fraud-decision-support — docs index

Read `00` first. Read the rest when the table says to.

**No file exists in `docs/` without a row here.** If you cannot write its
"read it when", it should not exist.

| Doc | Read it when |
| --- | --- |
| [`00-overview.md`](00-overview.md) | **Always first.** What this is, the non-negotiables, how state and render fit together, the decisions taken. |
| [`01-content-model.md`](01-content-model.md) | You are adding or changing a stage, node, answer, content block or context field. |

**Known gaps:** [`../STATE.md`](../STATE.md) is the register of deferred work
and decisions awaiting a human. If something here looks absent, check there
before assuming it was an oversight.
