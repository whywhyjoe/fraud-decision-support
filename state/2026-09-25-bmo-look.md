# State — BMO look

Last touched: 2026-09-25
Mode: Joe
Branch: `main` of `whywhyjoe/fraud-decision-support`, not pushed (committed locally)
State: first cut of the BMO-styled copy built, both test tiers green on both files; not yet reviewed by Joe.

## What this is

A second copy of the demo, `app/fraud-decision-support-bmo.html`, restyled
in the BMO look (the BMO SharePoint design system's palette, elevation and
motion) with a short branded header and a richer guidance column. The richer
column is there to show the client that guidance can carry headings,
bullets, step graphics and document thumbnails, not only plain text, which
they care about. The greyscale original is untouched; Joe chose a copy over
restyling it so the demo-build thread's deliverable stays as it was.

## Done

- The copy exists and passes `flow.test.mjs` and `smoke.mjs` with
  `APP=fraud-decision-support-bmo.html`. `LOG.md` has the list of changes.
- Content model additions are in `docs/01-content-model.md` under *Richer
  blocks*.

## Next

- [ ] Joe reviews the look in a browser. Likely points to settle: header
      height and photo crop (`--hero-h`, `--hero-photo-w`, `--hero-photo-pos`
      in `:root`), how busy the guidance column is, and whether the
      group-heading icons repeat the item icons too much.
- [ ] Push once Joe is happy.
- [ ] Decide which file the client demo uses. If the BMO copy wins, the
      demo-build thread's `README.md` walkthrough applies to it unchanged.

## Open questions

- **Should the original get the content 0.5 blocks too?** Right now only the
  BMO copy renders `brief`, `steps` and document cards, and only it has that
  content. Joe to decide; see the landmine on drift below.

## Landmines

- **Content now lives in two files.** Both carry a full `#flow-data` block.
  The BMO copy's is the original's 0.4 content plus the 0.5 blocks. An edit
  to a label, prompt, answer or guidance line must be made in both, or they
  drift. Nothing checks this.
- **No build step, on purpose.** The copy was assembled once by a scratch
  script (crop the photo, splice in content, copy icons from the design
  system sprite) that was not kept. Edit the HTML directly from here on.
- **The photo is a base64 token** (`--hero-photo`) on one ~55 KB line in
  `:root`. Never Read or Edit the whole file carelessly around it; target
  edits by unique strings. It is `hero-photo.jpg` from
  `bsp-design-system/handoffs/employee-security-hub/bundle/project/assets/`,
  cropped to 1100×524 at JPEG 0.72.
- **The smoke takes ~28s on the copy**, not 5s: Playwright waits for each
  rise animation and hover lift to settle. Not a hang.
- **Playwright is not installed globally on this machine.** The smoke ran
  with `NODE_PATH=C:/dev/repos/sp-traffic-analytics/tools/node_modules`
  (playwright 1.62, whose Chromium is already downloaded).
- **The first paint skips `scrollIntoView`** (`state.ui.intro`), or a short
  screen scrolls the new header out of view on load.
- **Icons are copied symbols** from `bsp-design-system/fluent-basic-icons.svg`,
  referenced as `#ic-fluent-<name>-24-regular`. A name not in the page's
  sprite renders blank; copy the symbol in.
- Hard rules still hold for the copy: 14px floor, no uppercase or
  letter-spacing, no pills, no coloured left borders, placeholder banner,
  tokens only in `:root`.
